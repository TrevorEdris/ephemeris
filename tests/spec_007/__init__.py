# Hook scope integration tests deprecated in v0.2.0 — hooks are no-ops now.
# scope.py itself still works; unit tests for it remain in this dir.
collect_ignore_glob = ["test_hook_scope_integration.py"]
